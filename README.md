# Instagram User Checker

This is a Python script that checks for available Instagram usernames and sends notifications via Telegram.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the script:
   ```bash
   python main.py
   ```

## Hosting on Railway

This repository includes a `Procfile` for easy deployment as a worker on Railway.
